import { FilterBornTodayPipe } from './filter-born-today.pipe';

describe('FilterBornTodayPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBornTodayPipe();
    expect(pipe).toBeTruthy();
  });
});
